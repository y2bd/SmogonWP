<phone:PhoneApplicationPage xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
                            xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            xmlns:bab="clr-namespace:BindableApplicationBar;assembly=BindableApplicationBar"
                            xmlns:gala="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Extras.WP8"
                            xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
                            xmlns:sys="clr-namespace:System;assembly=mscorlib"
                            xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                            xmlns:ec="clr-namespace:Microsoft.Expression.Interactivity.Core;assembly=Microsoft.Expression.Interactions"
                            xmlns:controls="clr-namespace:SmogonWP.Controls"
                            xmlns:eim="clr-namespace:Microsoft.Expression.Interactivity.Media;assembly=Microsoft.Expression.Interactions"
                            x:Class="SmogonWP.View.TeamBuilderView"
                            SupportedOrientations="Portrait"
                            Orientation="Portrait"
                            mc:Ignorable="d"
                            shell:SystemTray.IsVisible="True"
                            shell:SystemTray.Opacity="0">
    <phone:PhoneApplicationPage.Resources>
        <DataTemplate x:Key="TeamItemTemplate">
            <Grid x:Name="TeamItemContainerGrid"
                  Margin="0,12">
                <Grid.Resources>
                    <Storyboard x:Name="OnLoadedAnimation">
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)"
                                                       Storyboard.TargetName="textBlock">
                            <EasingDoubleKeyFrame KeyTime="0"
                                                  Value="0" />
                            <EasingDoubleKeyFrame KeyTime="0:0:0.3"
                                                  Value="1">
                                <EasingDoubleKeyFrame.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </EasingDoubleKeyFrame.EasingFunction>
                            </EasingDoubleKeyFrame>
                        </DoubleAnimationUsingKeyFrames>
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)"
                                                       Storyboard.TargetName="textBlock1">
                            <EasingDoubleKeyFrame KeyTime="0"
                                                  Value="0" />
                            <EasingDoubleKeyFrame KeyTime="0:0:0.05"
                                                  Value="0" />
                            <EasingDoubleKeyFrame KeyTime="0:0:0.35"
                                                  Value="1">
                                <EasingDoubleKeyFrame.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </EasingDoubleKeyFrame.EasingFunction>
                            </EasingDoubleKeyFrame>
                        </DoubleAnimationUsingKeyFrames>
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)"
                                                       Storyboard.TargetName="listBox">
                            <EasingDoubleKeyFrame KeyTime="0"
                                                  Value="0" />
                            <EasingDoubleKeyFrame KeyTime="0:0:0.1"
                                                  Value="0" />
                            <EasingDoubleKeyFrame KeyTime="0:0:0.4"
                                                  Value="1">
                                <EasingDoubleKeyFrame.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </EasingDoubleKeyFrame.EasingFunction>
                            </EasingDoubleKeyFrame>
                        </DoubleAnimationUsingKeyFrames>
                    </Storyboard>
                </Grid.Resources>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Loaded"
                                    SourceName="TeamItemContainerGrid">
                        <eim:ControlStoryboardAction x:Name="AnimateInTeamItem"
                                                     Storyboard="{StaticResource OnLoadedAnimation}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <StackPanel Orientation="Horizontal"
                            Margin="0,0,0,3">

                    <TextBlock x:Name="textBlock"
                               Text="{Binding Name}"
                               Style="{StaticResource PhoneTextTitle2Style}"
                               Margin="12,0" />

                    <TextBlock x:Name="textBlock1"
                               Text="{Binding TeamType}"
                               Style="{StaticResource PhoneTextSubtleStyle}"
                               VerticalAlignment="Bottom"
                               Margin="-6,0,0,4" />

                </StackPanel>

                <ListBox x:Name="listBox"
                         Grid.Row="1"
                         ItemsSource="{Binding Types}"
                         Margin="12,0"
                         FontFamily="Portable User Interface">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding BackgroundBrush}"
                                    Margin="0"
                                    Width="72"
                                    Height="6" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </DataTemplate>
    </phone:PhoneApplicationPage.Resources>

    <phone:PhoneApplicationPage.FontFamily>
        <StaticResource ResourceKey="PhoneFontFamilyNormal" />
    </phone:PhoneApplicationPage.FontFamily>
    <phone:PhoneApplicationPage.FontSize>
        <StaticResource ResourceKey="PhoneFontSizeNormal" />
    </phone:PhoneApplicationPage.FontSize>
    <phone:PhoneApplicationPage.Foreground>
        <StaticResource ResourceKey="PhoneForegroundBrush" />
    </phone:PhoneApplicationPage.Foreground>
    <phone:PhoneApplicationPage.DataContext>
        <Binding Path="TeamBuilder"
                 Source="{StaticResource Locator}" />
    </phone:PhoneApplicationPage.DataContext>

    <bab:Bindable.ApplicationBar>
        <bab:BindableApplicationBar ButtonsSource="{Binding MenuButtons}"
                                    Mode="Default" />
    </bab:Bindable.ApplicationBar>

    <shell:SystemTray.ProgressIndicator>
        <shell:ProgressIndicator IsIndeterminate="True"
                                 IsVisible="{Binding TrayService.IsLoading}"
                                 Text="{Binding TrayService.CurrentMessage}" />
    </shell:SystemTray.ProgressIndicator>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="BackKeyPress">
            <gala:EventToCommand PassEventArgsToCommand="True"
                                 Command="{Binding BackKeyCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot"
          Background="Transparent">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="AddTeamControl">
                <VisualStateGroup.Transitions>
                    <VisualTransition GeneratedDuration="0:0:0.5">
                		<VisualTransition.GeneratedEasingFunction>
                			<BackEase EasingMode="EaseInOut" Amplitude="0.5"/>
                		</VisualTransition.GeneratedEasingFunction>
                	</VisualTransition>
                </VisualStateGroup.Transitions>
                <VisualState x:Name="Visible">
                    <Storyboard Completed="AddVisible_Completed">
                        <DoubleAnimation Duration="0"
                                         To="0.595"
                                         Storyboard.TargetProperty="(UIElement.Opacity)"
                                         Storyboard.TargetName="TeamListShade"
                                         d:IsOptimized="True" />
                        <DoubleAnimation Duration="0"
                                         To="1"
                                         Storyboard.TargetProperty="(UIElement.Opacity)"
                                         Storyboard.TargetName="AddPanel"
                                         d:IsOptimized="True" />
                        <DoubleAnimation Duration="0"
                                         To="0"
                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.TranslateY)"
                                         Storyboard.TargetName="ContentPanel"
                                         d:IsOptimized="True" />
                        <DoubleAnimation Duration="0" To="0" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.TranslateY)" Storyboard.TargetName="AddPanel" d:IsOptimized="True"/>
                    </Storyboard>
                </VisualState>
                <VisualState x:Name="Collapsed">
                    <Storyboard>
                        <DoubleAnimation Duration="0"
                                         To="0"
                                         Storyboard.TargetProperty="(UIElement.Opacity)"
                                         Storyboard.TargetName="TeamListShade"
                                         d:IsOptimized="True" />
                    	<DoubleAnimation Duration="0"
                    		To="0"
                    		Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.TranslateY)"
                    		Storyboard.TargetName="AddPanel"
                    		d:IsOptimized="True" />
                        <DoubleAnimation Duration="0" To="0" Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="AddPanel" d:IsOptimized="True"/>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="AddPanel">
                        	<DiscreteObjectKeyFrame KeyTime="0">
                        		<DiscreteObjectKeyFrame.Value>
                        			<Visibility>Collapsed</Visibility>
                        		</DiscreteObjectKeyFrame.Value>
                        	</DiscreteObjectKeyFrame>
                        </ObjectAnimationUsingKeyFrames>
                        <DoubleAnimation Duration="0" To="0" Storyboard.TargetProperty="(UIElement.Projection).(PlaneProjection.RotationY)" Storyboard.TargetName="AddPanel" d:IsOptimized="True"/>
                        <DoubleAnimation Duration="0" To="-140" Storyboard.TargetProperty="(UIElement.Projection).(PlaneProjection.RotationX)" Storyboard.TargetName="AddPanel" d:IsOptimized="True"/>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <i:Interaction.Triggers>
            <ec:DataTrigger Binding="{Binding TeamAddState}"
                            Comparison="Equal"
                            Value="1">
                <ec:GoToStateAction x:Name="ShowAddPanel"
                                    StateName="Visible" />
            </ec:DataTrigger>
            <ec:DataTrigger Binding="{Binding TeamAddState}"
                            Comparison="Equal"
                            Value="0">
                <ec:GoToStateAction x:Name="HideAddPanel"
                                    StateName="Collapsed" />
            </ec:DataTrigger>
        </i:Interaction.Triggers>

        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="#22CCCCCC"
                Grid.RowSpan="3" />

        <!-- this is so the addpanel has something to slide out under from -->

        <StackPanel Grid.Row="1"
                    Margin="12,6,0,12">
            <TextBlock x:Name="PageTitle"
                       Text="TEAM MANAGER"
                       Style="{StaticResource PhoneTextTitle2Style}" />
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel"
              Grid.Row="2"
              Margin="12,0,12,0"
              CacheMode="BitmapCache"
              RenderTransformOrigin="0.5,0.5">
            <Grid.RenderTransform>
                <CompositeTransform />
            </Grid.RenderTransform>
            <TextBlock x:Name="NoTeamsText"
                       Style="{StaticResource EmptyListHeaderStyle}"
                       TextWrapping="Wrap"
                       Margin="12,6,12,0"
                       Opacity="0">
				<Run Text="You haven't created any teams yet! Press the plus below to make one." />
            </TextBlock>

            <controls:BindableLongListSelector x:Name="TeamSelector"
                                               ItemsSource="{Binding Teams}"
                                               ItemTemplate="{StaticResource TeamItemTemplate}" />

            <Border x:Name="TeamListShade"
                    BorderBrush="Black"
                    BorderThickness="1"
                    Background="{StaticResource PhoneBackgroundBrush}"
                    Margin="-12,0"
                    Opacity="0" />
        </Grid>
        <Grid x:Name="AddPanel"
              Grid.Row="2"
              CacheMode="BitmapCache"
              Opacity="0"
              RenderTransformOrigin="0.5,0.5"
              Background="{StaticResource PhoneBackgroundBrush}"
              VerticalAlignment="Top">
        	<Grid.Projection>
        		<PlaneProjection/>
        	</Grid.Projection>
            <Grid.RenderTransform>
                <CompositeTransform TranslateY="-210" />
            </Grid.RenderTransform>

        	<Border Background="#22CCCCCC" />

        	<StackPanel Margin="12,12,12,0">
        		<TextBlock Text="Name your new team"
        			Style="{StaticResource PhoneTextNormalStyle}"
        			Foreground="{StaticResource PhoneSubtleBrush}"
        			Margin="12,0,12,-6" />
        		<TextBox x:Name="TeamNameInput"
        			Margin="0,0,0,12"
        			InputScope="Text"
        			KeyUp="TeamNameInput_OnKeyUp" />

        		<toolkit:PickerBox x:Name="TeamTypePicker"
        			Margin="0,0,0,12"
        			Header="Choose a format"
        			FontFamily="Portable User Interface"
        			SelectedIndex="0">
        			<sys:String>singles</sys:String>
        			<sys:String>doubles</sys:String>
        			<sys:String>triples</sys:String>
        			<sys:String>rotation</sys:String>
        		</toolkit:PickerBox>

        	</StackPanel>
        </Grid>
    </Grid>

</phone:PhoneApplicationPage>